Installs docker, python3, and VS Code
The current versions of the respective packages will be installed. If any modifications to package versions need to happen. 
Run the corresponding command(s) for that to happen.
Run any package-specific updates that one may need.

